[project]
name = "mathformula2tex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dashscope>=1.25.12",
    "dotenv>=0.9.9",
    "pytest>=9.0.2",
]
